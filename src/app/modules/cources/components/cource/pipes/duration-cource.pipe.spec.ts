import { DurationCourcePipe } from './duration-cource.pipe';

describe('DurationCourcePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationCourcePipe();
    expect(pipe).toBeTruthy();
  });
});
